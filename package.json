{
  "name": "beach-simulator-terrain-base",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test": "node --test",
    "verify:jam": "npm run build && node scripts/verify-jam-build.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  }
}
